<!DOCTYPE html>
<html>
  <head>
    <title>LibraryApp</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>

    <div class="header">
      <h1> Welcome in Vade Mecum Bibliotheca </h1>
      <div class="container">
        <h2><%= link_to "Main", root_path %></h2>

        <div class="nav">
          
          <% if current_user %>

           <ul>
            <li><%= current_user.email %></li>
            <li><%= button_to "Log Out", destroy_user_session_path, method: :delete %></li>
           </ul>

          <% else %>

           <ul>
             <li><%= link_to "Login", 'login' %></li>
             <li><%= link_to "Signup", 'signup' %></li>
           </ul>
           
          <% end %>
        </div>
      </div>
    </div>

    <%= yield %>
    
  </body>
</html>
